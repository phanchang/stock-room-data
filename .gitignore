# --- 忽略 Python 快取與環境 ---
__pycache__/
*.py[cod]
*$py.class
.venv/
env/
.env

# --- 忽略日誌檔 (保留你原本的要求) ---
utils/logs/*.log
*.log

# --- 資料夾設定 ---
# 1. 先忽略整個 data 夾 (保險起見)
# 忽略所有在 data 底下的東西
data/*

# 但是「不要」忽略 data/cache 這個資料夾本身 (重要！)
!data/cache/

# 也不要忽略 cache 底下的子資料夾 (tw, us)
!data/cache/*/

# 最後，不要忽略所有的 parquet 檔案
!data/cache/**/*.parquet

# 3. 如果你有 metadata 夾 (存放 summary.json)，也不要忽略
!data/cache/metadata/*.json