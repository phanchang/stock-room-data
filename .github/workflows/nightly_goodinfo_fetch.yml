name: Nightly Goodinfo Fetch
on:
  schedule:
    - cron: '0 20 * * *' # 台北 04:00
  workflow_dispatch:

jobs:
  fetch-goodinfo:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Set up Python
        uses: actions/setup-python@v4
        with: {python-version: '3.11'}
      - name: Install dependencies
        run: pip install pandas numpy selenium webdriver-manager beautifulsoup4 python-dotenv requests lxml html5lib
      - name: Revenue High (Critical Timeout)
        continue-on-error: true
        run: python scripts/crawler_goodinfo_revenue_high.py --mode fetch
      - name: Holder Change
        continue-on-error: true
        run: python scripts/crawler_goodinfo_holder_change.py --mode fetch
      - name: Commit and Push Results
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add data/goodinfo/
          git commit -m "Auto: Update Goodinfo Data" || echo "No changes to commit"
          git pull --rebase origin main  # 🟢 加入這行：先拉回遠端更新，避免衝突
          git push origin main